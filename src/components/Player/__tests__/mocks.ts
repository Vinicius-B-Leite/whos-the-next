import { PlayerType } from "@/types/Player";

const playerMock: PlayerType = {
    id: '123321',
    losses: 0,
    playerName: 'Test',
    wins: 0,
    avatarUrl: ''
}

export const mock = {
    playerMock
}