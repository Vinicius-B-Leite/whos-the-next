import { TextType } from "@/components/Text";
import { responsiveSize } from "@/theme/responsiveSize";

export const pontuationsStyle: TextType = {
    fontWeight: 'bold',
    color: 'primaryText',
    marginHorizontal: 10,
    fontSize: responsiveSize[20]
}